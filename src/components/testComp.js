import React, { Component } from 'react'

export default class TestComp extends Component {
    render() {
        const {gearList} = this.props
        return (
            <div>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
<select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
                <select className='w3-select' style={{width: '300px'}}>
                    {gearList.ammo.map((value, keyIndex) => { 
                        return(<option key={keyIndex} value={value}>{value.name}</option>);
                    })}
                </select>
            </div>
        )
    }
}
